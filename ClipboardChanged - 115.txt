; ===========================================
; 完整修复版本 - 文件监听系统
; ===========================================

; 定义全局变量
Global IsCopying, ClipboardLock, retryCount, textData, GlobalFileList, SQLiteDB, DBPath, ProcessingContext
Global TransactionActive := false
Global TransactionOwner := ""

; 初始化全局重试计数
retryCount := 0
textData := ""
ProcessingContext := ""

; 记录文件夹和文件状态
FolderStates := {}

; ===========================================
; 主监听函数
; ===========================================
WatchDatabaseDir:
    Global SQLiteDB, DBPath, ProcessingContext, TransactionActive, TransactionOwner, FolderStates
    ProcessingContext := "WatchDir"
    
    if (!CheckDBUnlocked(DBPath)) {
        MsgBox, 16, 错误, 错误 ID：503643 数据库被占用，请稍后再试
        ProcessingContext := ""
        Return
    }
    
    if (!SQLiteDB) {
        SQLiteDB := New SQLiteDB()
        if (!SQLiteDB.OpenDB(DBPath)) {
            ErrorMsg := SQLiteDB._ErrMsg()
            MsgBox, 16, 错误, 错误 ID：525077 打开数据库失败: %ErrorMsg%
            ProcessingContext := ""
            Return
        }
    }
    
    try {
        ; === 先检查注册登记路径表的结构 ===
        CheckTableSQL := "SELECT name FROM sqlite_master WHERE type='table' AND name='注册登记路径';"
        CheckTableResult := {}
        TableExists := false
        if (SQLiteDB.GetTable(CheckTableSQL, CheckTableResult) && CheckTableResult.RowCount > 0) {
            TableExists := true
        }
        
        ; 确保注册登记路径表存在（修复：使用 Local_ID）
        if (!TableExists) {
            CreateTableSQL := "CREATE TABLE IF NOT EXISTS 注册登记路径 (Local_ID INTEGER PRIMARY KEY AUTOINCREMENT, 标识 TEXT, 文件夹或文件名称 TEXT, 路径 TEXT NOT NULL UNIQUE, 文件夹大小 TEXT, 详细资料 TEXT DEFAULT '', 文件数量 INTEGER, 访问时间 DATETIME DEFAULT (datetime('now', 'localtime')));"
            if (!SQLiteDB.Exec(CreateTableSQL)) {
                ErrorMsg := SQLiteDB._ErrMsg()
                MsgBox, 16, 错误, 错误 ID：212116 创建注册登记路径表失败: %ErrorMsg%
                ProcessingContext := ""
                Return
            }
        }
        
        ; 检查Database表
        CheckSQL := "SELECT name FROM sqlite_master WHERE type='table' AND name='Database';"
        CheckResult := {}
        if (!SQLiteDB.GetTable(CheckSQL, CheckResult) || CheckResult.RowCount = 0) {
            MsgBox, 16, 错误, 错误 ID：212114 Database表不存在
            ProcessingContext := ""
            Return
        }
        
        ; 构建监听目录列表
        dirList := []
        dirList.Push(A_ScriptDir . "\Database")
        
        ; === 从分类字典_One表读取监听文件夹（主要方式）===
        sqlMonitorFolders := "SELECT Category_Sub FROM 分类字典_One WHERE Category_Main = '监听文件夹' AND Flag_Status = 1;"
        ResultMonitor := {}
        if (SQLiteDB.GetTable(sqlMonitorFolders, ResultMonitor) && ResultMonitor.RowCount >= 1) {
            Loop, % ResultMonitor.RowCount {
                folderPath := ResultMonitor.Rows[A_Index][1]
                if (folderPath = "") {
                    continue
                }
                found := false
                for index, dir in dirList {
                    if (dir = folderPath) {
                        found := true
                        break
                    }
                }
                if (!found && FileExist(folderPath)) {
                    dirList.Push(folderPath)
                }
            }
        }
        
        ; === 兼容旧的属性表（如果还在使用）===
        sqlFolders := "SELECT 属性值 FROM 属性 WHERE 属性格式 = '文件夹路径';"
        ResultFolders := {}
        if (SQLiteDB.GetTable(sqlFolders, ResultFolders) && ResultFolders.RowCount >= 1) {
            Loop, % ResultFolders.RowCount {
                folderPath := ResultFolders.Rows[A_Index][1]
                found := false
                for index, dir in dirList {
                    if (dir = folderPath) {
                        found := true
                        break
                    }
                }
                if (!found && FileExist(folderPath)) {
                    dirList.Push(folderPath)
                }
            }
        }
        
        ; 处理所有监听目录
        for dirIndex, dirPath in dirList {
            if (!FileExist(dirPath)) {
                continue
            }
            
            ; 处理子文件夹
            Loop, Files, %dirPath%\*, D R
            {
                item := A_LoopFileFullPath
                SB_SetText("正在处理子文件夹: " . item)
                ProcessSingleFolder(item)
            }
            
            ; 处理直属文件
            Loop, Files, %dirPath%\*, F
            {
                item := A_LoopFileFullPath
                if (FileExist(item) && !InStr(FileExist(item), "D")) {
                    SplitPath, item, fileName
                    SB_SetText("正在处理直属文件: " . fileName)
                    ProcessSingleFile(item)
                }
            }
        }
        
        RegisterAndValidateFolders()
        ValidatePaths()
        FixNullSortValues("")
        SB_SetText("监听处理完成 - 已处理 " . dirList.Length() . " 个监听目录")
        
    } catch e {
        ErrorMsg := e.Message
        MsgBox, 16, 错误, 错误 ID：503644 WatchDatabaseDir异常: %ErrorMsg%
    } finally {
        if (TransactionActive && TransactionOwner = "WatchDatabaseDir") {
            RollbackSafeTransaction("WatchDatabaseDir")
        }
        ProcessingContext := ""
    }
Return

; ===========================================
; 处理单个文件夹
; ===========================================
ProcessSingleFolder(folderPath) {
    Global SQLiteDB, FolderStates, ProcessingContext
    
    if (!SQLiteDB) {
        MsgBox, 16, 错误, 错误 ID：717798 数据库连接未初始化
        Return false
    }
    
    if (!FileExist(folderPath) || !InStr(FileExist(folderPath), "D")) {
        Return false
    }
    
    folderState := GetFolderState(folderPath)
    SplitPath, folderPath, folderName
    sizeText := FormatSize(folderState.totalSize)
    
    if (ProcessingContext = "WatchDir") {
        transactionStarted := BeginSafeTransaction("ProcessSingleFolder")
        if (!transactionStarted) {
            MsgBox, 16, 错误, 错误 ID：212610 事务开始失败
            Return false
        }
        
        try {
            ; 检查文件夹路径是否已登记
            CheckFolderSQL := "SELECT Local_ID FROM 注册登记路径 WHERE 路径 = ?;"
            CheckFolderResult := {}
            if (!SQLiteDB.GetTable(CheckFolderSQL, CheckFolderResult, [folderPath])) {
                ErrorMsg := SQLiteDB._ErrMsg()
                MsgBox, 16, 错误, 错误 ID：212611 查询文件夹失败: %ErrorMsg%
                RollbackSafeTransaction("ProcessSingleFolder")
                Return false
            }
            
            if (CheckFolderResult.RowCount = 0) {
                ; === 路径未登记，需要处理 ===
                
                ; 1. 先登记路径（Local_ID 自增，不需要手动指定）
                InsertFolderSQL := "INSERT INTO 注册登记路径 (标识, 文件夹或文件名称, 路径, 文件夹大小, 详细资料, 文件数量, 访问时间) VALUES (?, ?, ?, ?, ?, ?, datetime('now', 'localtime'));"
                InsertSuccess := false
                if (SQLiteDB.ExecuteWithParams) {
                    InsertSuccess := SQLiteDB.ExecuteWithParams(InsertFolderSQL, ["📁", folderName, folderPath, sizeText, folderState.fileList, folderState.fileCount])
                } else {
                    EscapedFolderName := StrReplace(folderName, "'", "''")
                    EscapedFolderPath := StrReplace(folderPath, "'", "''")
                    EscapedSizeText := StrReplace(sizeText, "'", "''")
                    EscapedFileList := StrReplace(folderState.fileList, "'", "''")
                    FallbackSQL := "INSERT INTO 注册登记路径 (标识, 文件夹或文件名称, 路径, 文件夹大小, 详细资料, 文件数量, 访问时间) VALUES ('📁', '" . EscapedFolderName . "', '" . EscapedFolderPath . "', '" . EscapedSizeText . "', '" . EscapedFileList . "', " . folderState.fileCount . ", datetime('now', 'localtime'));"
                    InsertSuccess := SQLiteDB.Exec(FallbackSQL)
                }
                
                if (!InsertSuccess) {
                    ErrorMsg := SQLiteDB._ErrMsg()
                    MsgBox, 16, 错误, 错误 ID：212630 登记文件夹失败: %ErrorMsg%
                    RollbackSafeTransaction("ProcessSingleFolder")
                    Return false
                }
                
                ; 2. 保存状态
                FolderStates[folderPath] := folderState
                
                ; 3. 同步到Database主表（这才是真正的数据）
                GlobalFileList := folderPath
                GlobalComment := folderName
                if (Gosub_SavePathMerged_Success()) {
                    FolderStates[folderPath].processedTime := A_Now
                }
                
            } else {
                ; === 路径已登记，只更新登记表的元数据 ===
                needsUpdate := false
                if (!FolderStates.HasKey(folderPath)) {
                    needsUpdate := true
                } else {
                    oldState := FolderStates[folderPath]
                    if (folderState.fileCount != oldState.fileCount || folderState.totalSize != oldState.totalSize || folderState.lastModified != oldState.lastModified) {
                        needsUpdate := true
                    }
                }
                
                if (needsUpdate) {
                    UpdateFolderSQL := "UPDATE 注册登记路径 SET 标识 = ?, 文件夹或文件名称 = ?, 文件夹大小 = ?, 详细资料 = ?, 文件数量 = ?, 访问时间 = datetime('now', 'localtime') WHERE 路径 = ?;"
                    UpdateSuccess := false
                    if (SQLiteDB.ExecuteWithParams) {
                        UpdateSuccess := SQLiteDB.ExecuteWithParams(UpdateFolderSQL, ["📁", folderName, sizeText, folderState.fileList, folderState.fileCount, folderPath])
                    } else {
                        EscapedFolderName := StrReplace(folderName, "'", "''")
                        EscapedSizeText := StrReplace(sizeText, "'", "''")
                        EscapedFileList := StrReplace(folderState.fileList, "'", "''")
                        EscapedFolderPath := StrReplace(folderPath, "'", "''")
                        FallbackSQL := "UPDATE 注册登记路径 SET 标识 = '📁', 文件夹或文件名称 = '" . EscapedFolderName . "', 文件夹大小 = '" . EscapedSizeText . "', 详细资料 = '" . EscapedFileList . "', 文件数量 = " . folderState.fileCount . ", 访问时间 = datetime('now', 'localtime') WHERE 路径 = '" . EscapedFolderPath . "';"
                        UpdateSuccess := SQLiteDB.Exec(FallbackSQL)
                    }
                    
                    if (!UpdateSuccess) {
                        ErrorMsg := SQLiteDB._ErrMsg()
                        MsgBox, 16, 错误, 错误 ID：212660 更新文件夹登记失败: %ErrorMsg%
                        RollbackSafeTransaction("ProcessSingleFolder")
                        Return false
                    }
                    
                    FolderStates[folderPath] := folderState
                }
                ; 注意：已登记的路径不再同步到Database表，避免重复插入
            }
            
            if (!CommitSafeTransaction("ProcessSingleFolder")) {
                ErrorMsg := SQLiteDB._ErrMsg()
                MsgBox, 16, 错误, 错误 ID：212640 提交事务失败: %ErrorMsg%
                RollbackSafeTransaction("ProcessSingleFolder")
                Return false
            }
        } catch e {
            ErrorMsg := e.Message
            MsgBox, 16, 错误, 错误 ID：212641 ProcessSingleFolder异常: %ErrorMsg%
            RollbackSafeTransaction("ProcessSingleFolder")
            Return false
        }
    } else if (ProcessingContext = "Clipboard") {
        ProcessFolderToHistoryTable(folderPath)
    }
    
    Return true
}

; ===========================================
; 处理单个文件
; ===========================================
ProcessSingleFile(filePath) {
    Global SQLiteDB, DBPath, FolderStates, ProcessingContext
    
    if (!FileExist(filePath) || InStr(FileExist(filePath), "D")) {
        Return false
    }
    
    if (!CheckDBUnlocked(DBPath)) {
        MsgBox, 16, 错误, 错误 ID：212505 数据库被锁定
        Return false
    }
    
    if (!SQLiteDB) {
        SQLiteDB := New SQLiteDB()
        if (!SQLiteDB.OpenDB(DBPath)) {
            ErrorMsg := SQLiteDB._ErrMsg()
            MsgBox, 16, 错误, 错误 ID：4122461 打开数据库失败: %ErrorMsg%
            Return false
        }
    }
    
    FileGetSize, fileSize, %filePath%
    FileGetTime, fileTime, %filePath%, M
    SplitPath, filePath, fileName, , fileExt
    
    Identifier := GetFileTypeSymbol(filePath, fileExt)
    sizeText := FormatSize(fileSize)
    
    try {
        if (ProcessingContext = "WatchDir") {
            transactionStarted := BeginSafeTransaction("ProcessSingleFile")
            if (!transactionStarted) {
                MsgBox, 16, 错误, 错误 ID：212510 事务开始失败
                Return false
            }
            
            try {
                ; 检查文件路径是否已登记
                CheckFileSQL := "SELECT Local_ID FROM 注册登记路径 WHERE 路径 = ?;"
                CheckFileResult := {}
                if (!SQLiteDB.GetTable(CheckFileSQL, CheckFileResult, [filePath])) {
                    ErrorMsg := SQLiteDB._ErrMsg()
                    MsgBox, 16, 错误, 错误 ID：212511 查询文件失败: %ErrorMsg%
                    RollbackSafeTransaction("ProcessSingleFile")
                    Return false
                }
                
                if (CheckFileResult.RowCount = 0) {
                    ; === 路径未登记，需要处理 ===
                    
                    ; 1. 先登记路径（Local_ID 自增，不需要手动指定）
                    InsertFileSQL := "INSERT INTO 注册登记路径 (标识, 文件夹或文件名称, 路径, 文件夹大小, 详细资料, 文件数量, 访问时间) VALUES (?, ?, ?, ?, ?, ?, datetime('now', 'localtime'));"
                    InsertSuccess := false
                    if (SQLiteDB.ExecuteWithParams) {
                        InsertSuccess := SQLiteDB.ExecuteWithParams(InsertFileSQL, [Identifier, fileName, filePath, sizeText, filePath, 1])
                    } else {
                        EscapedIdentifier := StrReplace(Identifier, "'", "''")
                        EscapedFileName := StrReplace(fileName, "'", "''")
                        EscapedFilePath := StrReplace(filePath, "'", "''")
                        EscapedSizeText := StrReplace(sizeText, "'", "''")
                        FallbackSQL := "INSERT INTO 注册登记路径 (标识, 文件夹或文件名称, 路径, 文件夹大小, 详细资料, 文件数量, 访问时间) VALUES ('" . EscapedIdentifier . "', '" . EscapedFileName . "', '" . EscapedFilePath . "', '" . EscapedSizeText . "', '" . EscapedFilePath . "', 1, datetime('now', 'localtime'));"
                        InsertSuccess := SQLiteDB.Exec(FallbackSQL)
                    }
                    
                    if (!InsertSuccess) {
                        ErrorMsg := SQLiteDB._ErrMsg()
                        MsgBox, 16, 错误, 错误 ID：212530 登记文件失败: %ErrorMsg%
                        RollbackSafeTransaction("ProcessSingleFile")
                        Return false
                    }
                    
                    ; 2. 保存状态
                    FolderStates[filePath] := {totalSize: fileSize, lastModified: fileTime, fileCount: 1, fileList: filePath}
                    
                    ; 3. 同步到Database主表（这才是真正的数据）
                    ProcessFileToDatabase(filePath, fileName, fileExt, fileSize)
                    
                } else {
                    ; === 路径已登记，只更新登记表的元数据 ===
                    needsUpdate := false
                    if (!FolderStates.HasKey(filePath)) {
                        needsUpdate := true
                    } else {
                        oldState := FolderStates[filePath]
                        if (fileSize != oldState.totalSize || fileTime != oldState.lastModified) {
                            needsUpdate := true
                        }
                    }
                    
                    if (needsUpdate) {
                        UpdateFileSQL := "UPDATE 注册登记路径 SET 标识 = ?, 文件夹或文件名称 = ?, 文件夹大小 = ?, 详细资料 = ?, 文件数量 = 1, 访问时间 = datetime('now', 'localtime') WHERE 路径 = ?;"
                        UpdateSuccess := false
                        if (SQLiteDB.ExecuteWithParams) {
                            UpdateSuccess := SQLiteDB.ExecuteWithParams(UpdateFileSQL, [Identifier, fileName, sizeText, filePath, filePath])
                        } else {
                            EscapedIdentifier := StrReplace(Identifier, "'", "''")
                            EscapedFileName := StrReplace(fileName, "'", "''")
                            EscapedSizeText := StrReplace(sizeText, "'", "''")
                            EscapedFilePath := StrReplace(filePath, "'", "''")
                            FallbackSQL := "UPDATE 注册登记路径 SET 标识 = '" . EscapedIdentifier . "', 文件夹或文件名称 = '" . EscapedFileName . "', 文件夹大小 = '" . EscapedSizeText . "', 详细资料 = '" . EscapedFilePath . "', 文件数量 = 1, 访问时间 = datetime('now', 'localtime') WHERE 路径 = '" . EscapedFilePath . "';"
                            UpdateSuccess := SQLiteDB.Exec(FallbackSQL)
                        }
                        
                        if (!UpdateSuccess) {
                            ErrorMsg := SQLiteDB._ErrMsg()
                            MsgBox, 16, 错误, 错误 ID：212560 更新文件登记失败: %ErrorMsg%
                            RollbackSafeTransaction("ProcessSingleFile")
                            Return false
                        }
                        
                        FolderStates[filePath] := {totalSize: fileSize, lastModified: fileTime, fileCount: 1, fileList: filePath}
                    }
                    ; 注意：已登记的路径不再同步到Database表，避免重复插入
                }
                
                if (!CommitSafeTransaction("ProcessSingleFile")) {
                    ErrorMsg := SQLiteDB._ErrMsg()
                    MsgBox, 16, 错误, 错误 ID：212540 提交事务失败: %ErrorMsg%
                    RollbackSafeTransaction("ProcessSingleFile")
                    Return false
                }
            } catch e {
                ErrorMsg := e.Message
                MsgBox, 16, 错误, 错误 ID：212541 ProcessSingleFile异常: %ErrorMsg%
                RollbackSafeTransaction("ProcessSingleFile")
                Return false
            }
        } else if (ProcessingContext = "Clipboard") {
            ProcessFileToHistoryTable(filePath, fileName, fileExt, fileSize)
        }
    } catch e {
        eMessage := e.Message
        MsgBox, 16, 错误, 错误 ID：212580 ProcessSingleFile异常: %eMessage%
        if (TransactionActive && TransactionOwner = "ProcessSingleFile") {
            RollbackSafeTransaction("ProcessSingleFile")
        }
        Return false
    }
    Return true
}

; ===========================================
; 辅助函数
; ===========================================

RegisterAndValidateFolders() {
    Global SQLiteDB
    try {
        CheckFoldersSQL := "SELECT 路径, 详细资料 FROM 注册登记路径;"
        CheckFoldersResult := {}
        if (SQLiteDB.GetTable(CheckFoldersSQL, CheckFoldersResult) && CheckFoldersResult.RowCount >= 1) {
            Loop, % CheckFoldersResult.RowCount {
                path := CheckFoldersResult.Rows[A_Index][1]
                details := CheckFoldersResult.Rows[A_Index][2]
                if (!FileExist(path)) {
                    DeleteSQL := "DELETE FROM 注册登记路径 WHERE 路径 = ?;"
                    if (!SQLiteDB.ExecuteWithParams(DeleteSQL, [path])) {
                        ErrorMsg := SQLiteDB._ErrMsg()
                        MsgBox, 16, 错误, 错误 ID：212117 删除无效路径失败: %ErrorMsg%
                    }
                    continue
                }
                if (InStr(FileExist(path), "D") && details != "") {
                    validDetails := ""
                    StringSplit, fileList, details, `n
                    Loop, %fileList0% {
                        filePath := fileList%A_Index%
                        if (FileExist(filePath) && !InStr(FileExist(filePath), "D")) {
                            validDetails .= (validDetails = "" ? "" : "`n") . filePath
                        }
                    }
                    if (validDetails != details) {
                        UpdateDetailsSQL := "UPDATE 注册登记路径 SET 详细资料 = ? WHERE 路径 = ?;"
                        if (!SQLiteDB.ExecuteWithParams(UpdateDetailsSQL, [validDetails, path])) {
                            ErrorMsg := SQLiteDB._ErrMsg()
                            MsgBox, 16, 错误, 错误 ID：212118 更新文件夹详细资料失败: %ErrorMsg%
                        }
                    }
                }
            }
        }
    } catch e {
        eMessage := e.Message
        MsgBox, 16, 错误, 错误 ID：212119 RegisterAndValidateFolders异常: %eMessage%
    }
}

GetFolderState(folderPath) {
    state := {}
    state.fileCount := 0
    state.totalSize := 0
    state.lastModified := 0
    state.fileList := ""
    
    Loop, Files, %folderPath%\*, F
    {
        filePath := A_LoopFileFullPath
        if (FileExist(filePath) && !InStr(FileExist(filePath), "D")) {
            state.fileList .= (state.fileList = "" ? "" : "`n") . filePath
            state.fileCount++
            
            FileGetSize, fileSize, %filePath%
            if (fileSize != "") {
                state.totalSize += fileSize
            }
            
            FileGetTime, fileTime, %filePath%, M
            if (fileTime > state.lastModified) {
                state.lastModified := fileTime
            }
        }
    }
    
    Return state
}

FormatSize(size) {
    if (size >= 1073741824)
        Return Round(size / 1073741824, 1) . " GB"
    else if (size >= 1048576)
        Return Round(size / 1048576, 1) . " MB"
    else if (size >= 1024)
        Return Round(size / 1024, 1) . " KB"
    else if (size > 0)
        Return size . " B"
    else
        Return "0 B"
}

; ===========================================
; 处理文件夹到历史数据表
; ===========================================
ProcessFolderToHistoryTable(folderPath) {
    Global SQLiteDB
    
    SplitPath, folderPath, folderName
    folderState := GetFolderState(folderPath)
    totalSize := folderState.totalSize
    validFiles := folderState.fileCount
    sizeText := FormatSize(totalSize)
    
    WinGet, ActiveID, ID, A
    WinGetTitle, WindowTitle, ahk_id %ActiveID%
    SourceWindow := WindowTitle ? WindowTitle : "未知窗口"
    
    Identifier := "📁"
    Format := "📁"
    Tag := ""
    PathOrLink := folderState.fileList
    Content := "[文件夹] 文件数量: " . validFiles . " | 大小: " . sizeText . "`n" . folderPath
    Remark := folderName
    HashValue := Crypt.Hash.Str(folderPath . totalSize . validFiles, "SHA256", "UTF-8")
    
    CheckHashSQL := "SELECT 1 FROM 历史数据 WHERE 哈希值 = ? LIMIT 1;"
    CheckHashResult := {}
    if (SQLiteDB.GetTable(CheckHashSQL, CheckHashResult, [HashValue]) && CheckHashResult.RowCount >= 1) {
        Return false
    }
    
    transactionStarted := BeginSafeTransaction("ProcessFolderToHistoryTable")
    
    try {
        InsertSQL := "INSERT INTO 历史数据 (哈希值, 字节, 数据来源窗口, 标签, 标识, 类型) VALUES (?, ?, ?, ?, ?, ?);"
        InsertSuccess := false
        if (SQLiteDB.ExecuteWithParams) {
            InsertSuccess := SQLiteDB.ExecuteWithParams(InsertSQL, [HashValue, sizeText, SourceWindow, Tag, Identifier, Format])
        } else {
            EscapedHashValue := StrReplace(HashValue, "'", "''")
            EscapedSizeText := StrReplace(sizeText, "'", "''")
            EscapedSourceWindow := StrReplace(SourceWindow, "'", "''")
            EscapedTag := StrReplace(Tag, "'", "''")
            EscapedIdentifier := StrReplace(Identifier, "'", "''")
            EscapedFormat := StrReplace(Format, "'", "''")
            FallbackSQL := "INSERT INTO 历史数据 (哈希值, 字节, 数据来源窗口, 标签, 标识, 类型) VALUES ('" . EscapedHashValue . "', '" . EscapedSizeText . "', '" . EscapedSourceWindow . "', '" . EscapedTag . "', '" . EscapedIdentifier . "', '" . EscapedFormat . "');"
            InsertSuccess := SQLiteDB.Exec(FallbackSQL)
        }
        
        if (!InsertSuccess) {
            ErrorMsg := SQLiteDB._ErrMsg()
            if (transactionStarted) {
                RollbackSafeTransaction("ProcessFolderToHistoryTable")
            }
            Return false
        }
        
        LastID := 0
        if (SQLiteDB.GetTable("SELECT last_insert_rowid();", Result)) {
            LastID := Result.Rows[1][1]
        } else {
            ErrorMsg := SQLiteDB._ErrMsg()
            if (transactionStarted) {
                RollbackSafeTransaction("ProcessFolderToHistoryTable")
            }
            Return false
        }
        
        UpdateSQL := "UPDATE 历史数据 SET 备注 = ?, 内容 = ?, [路径/链接] = ?, 修改时间 = datetime('now', 'localtime') WHERE ID = ?;"
        UpdateSuccess := false
        if (SQLiteDB.ExecuteWithParams) {
            UpdateSuccess := SQLiteDB.ExecuteWithParams(UpdateSQL, [Remark, Content, PathOrLink, LastID])
        } else {
            EscapedRemark := StrReplace(Remark, "'", "''")
            EscapedContent := StrReplace(Content, "'", "''")
            EscapedPathOrLink := StrReplace(PathOrLink, "'", "''")
            FallbackSQL := "UPDATE 历史数据 SET 备注 = '" . EscapedRemark . "', 内容 = '" . EscapedContent . "', [路径/链接] = '" . EscapedPathOrLink . "', 修改时间 = datetime('now', 'localtime') WHERE ID = " . LastID . ";"
            UpdateSuccess := SQLiteDB.Exec(FallbackSQL)
        }
        
        if (!UpdateSuccess) {
            ErrorMsg := SQLiteDB._ErrMsg()
            if (transactionStarted) {
                RollbackSafeTransaction("ProcessFolderToHistoryTable")
            }
            Return false
        }
        
        if (transactionStarted && !CommitSafeTransaction("ProcessFolderToHistoryTable")) {
            ErrorMsg := SQLiteDB._ErrMsg()
            RollbackSafeTransaction("ProcessFolderToHistoryTable")
            Return false
        }
        
        SB_SetText("已保存文件夹到历史数据表 - 文件夹: " . folderName)
    } catch e {
        ErrorMsg := e.message
        if (transactionStarted) {
            RollbackSafeTransaction("ProcessFolderToHistoryTable")
        }
        Return false
    }
    
    Return true
}

; ===========================================
; 处理文件到历史数据表
; ===========================================
ProcessFileToHistoryTable(filePath, fileName, fileExt, fileSize) {
    Global SQLiteDB
    
    sizeText := FormatSize(fileSize)
    Identifier := GetFileTypeSymbol(filePath, fileExt)
    musicExts := "mp3,aac,m4a,ogg,flac,alac,ape,wav,aiff,dsf,dff,mid"
    Format := (InStr(musicExts, fileExt) || (fileExt = "m4a" && InStr(filePath, "ALAC"))) ? ("📎, " . Identifier . ", " . fileExt) : ("📎, " . (fileExt ? fileExt : "unknown"))
    
    WinGet, ActiveID, ID, A
    WinGetTitle, WindowTitle, ahk_id %ActiveID%
    SourceWindow := WindowTitle ? WindowTitle : "未知窗口"
    
    Tag := ""
    PathOrLink := filePath
    Content := "[文件] " . fileName . " | 大小: " . sizeText
    Remark := fileName
    HashValue := Crypt.Hash.Str(filePath . fileSize, "SHA256", "UTF-8")
    
    CheckHashSQL := "SELECT 1 FROM 历史数据 WHERE 哈希值 = ? LIMIT 1;"
    CheckHashResult := {}
    if (SQLiteDB.GetTable(CheckHashSQL, CheckHashResult, [HashValue]) && CheckHashResult.RowCount >= 1) {
        Return false
    }
    
    transactionStarted := BeginSafeTransaction("ProcessFileToHistoryTable")
    
    try {
        InsertSQL := "INSERT INTO 历史数据 (哈希值, 字节, 数据来源窗口, 标签, 标识, 类型) VALUES (?, ?, ?, ?, ?, ?);"
        InsertSuccess := false
        if (SQLiteDB.ExecuteWithParams) {
            InsertSuccess := SQLiteDB.ExecuteWithParams(InsertSQL, [HashValue, sizeText, SourceWindow, Tag, Identifier, Format])
        } else {
            EscapedHashValue := StrReplace(HashValue, "'", "''")
            EscapedSizeText := StrReplace(sizeText, "'", "''")
            EscapedSourceWindow := StrReplace(SourceWindow, "'", "''")
            EscapedTag := StrReplace(Tag, "'", "''")
            EscapedIdentifier := StrReplace(Identifier, "'", "''")
            EscapedFormat := StrReplace(Format, "'", "''")
            FallbackSQL := "INSERT INTO 历史数据 (哈希值, 字节, 数据来源窗口, 标签, 标识, 类型) VALUES ('" . EscapedHashValue . "', '" . EscapedSizeText . "', '" . EscapedSourceWindow . "', '" . EscapedTag . "', '" . EscapedIdentifier . "', '" . EscapedFormat . "');"
            InsertSuccess := SQLiteDB.Exec(FallbackSQL)
        }
        
        if (!InsertSuccess) {
            ErrorMsg := SQLiteDB._ErrMsg()
            if (transactionStarted) {
                RollbackSafeTransaction("ProcessFileToHistoryTable")
            }
            Return false
        }
        
        LastID := 0
        if (SQLiteDB.GetTable("SELECT last_insert_rowid();", Result)) {
            LastID := Result.Rows[1][1]
        } else {
            ErrorMsg := SQLiteDB._ErrMsg()
            if (transactionStarted) {
                RollbackSafeTransaction("ProcessFileToHistoryTable")
            }
            Return false
        }
        
        UpdateSQL := "UPDATE 历史数据 SET 备注 = ?, 内容 = ?, [路径/链接] = ?, 修改时间 = datetime('now', 'localtime') WHERE ID = ?;"
        UpdateSuccess := false
        if (SQLiteDB.ExecuteWithParams) {
            UpdateSuccess := SQLiteDB.ExecuteWithParams(UpdateSQL, [Remark, Content, PathOrLink, LastID])
        } else {
            EscapedRemark := StrReplace(Remark, "'", "''")
            EscapedContent := StrReplace(Content, "'", "''")
            EscapedPathOrLink := StrReplace(PathOrLink, "'", "''")
            FallbackSQL := "UPDATE 历史数据 SET 备注 = '" . EscapedRemark . "', 内容 = '" . EscapedContent . "', [路径/链接] = '" . EscapedPathOrLink . "', 修改时间 = datetime('now', 'localtime') WHERE ID = " . LastID . ";"
            UpdateSuccess := SQLiteDB.Exec(FallbackSQL)
        }
        
        if (!UpdateSuccess) {
            ErrorMsg := SQLiteDB._ErrMsg()
            if (transactionStarted) {
                RollbackSafeTransaction("ProcessFileToHistoryTable")
            }
            Return false
        }
        
        if (transactionStarted && !CommitSafeTransaction("ProcessFileToHistoryTable")) {
            ErrorMsg := SQLiteDB._ErrMsg()
            RollbackSafeTransaction("ProcessFileToHistoryTable")
            Return false
        }
        
        SB_SetText("已保存文件到历史数据表 - 文件: " . fileName)
    } catch e {
        ErrorMsg := e.message
        if (transactionStarted) {
            RollbackSafeTransaction("ProcessFileToHistoryTable")
        }
        Return false
    }
    
    Return true
}

; ===========================================
; 处理文件到Database表
; ===========================================
ProcessFileToDatabase(filePath, fileName, fileExt, fileSize) {
    Global SQLiteDB, FolderStates
    
    sizeText := FormatSize(fileSize)
    Identifier := GetFileTypeSymbol(filePath, fileExt)
    musicExts := "mp3,aac,m4a,ogg,flac,alac,ape,wav,aiff,dsf,dff,mid"
    Format := (InStr(musicExts, fileExt) || (fileExt = "m4a" && InStr(filePath, "ALAC"))) ? ("📎, " . Identifier . ", " . fileExt) : ("📎, " . (fileExt ? fileExt : "unknown"))
    textPreview := "[文件] " . fileName . " | 大小: " . sizeText
    groupName := ""
    SplitPath, filePath, , dirPath
    SplitPath, dirPath, groupName
    if (groupName = "") {
        groupName := "默认分组"
    }
    comment := fileName
    HashValue := Crypt.Hash.Str(filePath . fileSize, "SHA256", "UTF-8")
    
    baseFileName := fileName
    if (fileExt != "") {
        baseFileName := SubStr(fileName, 1, StrLen(fileName) - StrLen(fileExt) - 1)
    }
    
    CheckHashSQL := "SELECT 1 FROM Database WHERE 哈希值 = ? LIMIT 1;"
    CheckHashResult := {}
    if (SQLiteDB.GetTable(CheckHashSQL, CheckHashResult, [HashValue]) && CheckHashResult.RowCount >= 1) {
        SB_SetText("文件内容已存在，跳过插入: " . fileName)
        Return false
    }
    
    transactionStarted := BeginSafeTransaction("ProcessFileToDatabase_Insert")
    
    InsertSQL := "INSERT INTO Database (哈希值, 字节, 标签, 标识, 类型, 分组, 备注, 内容, 字符, 行数, 数量, 收藏, 禁删, 创建时间, 修改时间) VALUES (?, ?, ?, ?, ?, ?, ?, ?, 1, 1, 1, 1, 1, datetime('now', 'localtime'), datetime('now', 'localtime'));"
    InsertSuccess := false
    if (SQLiteDB.ExecuteWithParams) {
        InsertSuccess := SQLiteDB.ExecuteWithParams(InsertSQL, [HashValue, sizeText, baseFileName, Identifier, Format, groupName, comment, textPreview])
    } else {
        EscapedHashValue := StrReplace(HashValue, "'", "''")
        EscapedSizeText := StrReplace(sizeText, "'", "''")
        EscapedBaseFileName := StrReplace(baseFileName, "'", "''")
        EscapedIdentifier := StrReplace(Identifier, "'", "''")
        EscapedFormat := StrReplace(Format, "'", "''")
        EscapedGroupName := StrReplace(groupName, "'", "''")
        EscapedComment := StrReplace(comment, "'", "''")
        EscapedTextPreview := StrReplace(textPreview, "'", "''")
        FallbackSQL := "INSERT INTO Database (哈希值, 字节, 标签, 标识, 类型, 分组, 备注, 内容, 字符, 行数, 数量, 收藏, 禁删, 创建时间, 修改时间) VALUES ('" . EscapedHashValue . "', '" . EscapedSizeText . "', '" . EscapedBaseFileName . "', '" . EscapedIdentifier . "', '" . EscapedFormat . "', '" . EscapedGroupName . "', '" . EscapedComment . "', '" . EscapedTextPreview . "', 1, 1, 1, 1, 1, datetime('now', 'localtime'), datetime('now', 'localtime'));"
        InsertSuccess := SQLiteDB.Exec(FallbackSQL)
    }
    
    if (!InsertSuccess) {
        ErrorMsg := SQLiteDB._ErrMsg()
        MsgBox, 16, 错误, 错误 ID：212552 %ErrorMsg%
        if (transactionStarted) {
            RollbackSafeTransaction("ProcessFileToDatabase_Insert")
        }
        Return false
    }
    
    if (transactionStarted && !CommitSafeTransaction("ProcessFileToDatabase_Insert")) {
        ErrorMsg := SQLiteDB._ErrMsg()
        MsgBox, 16, 错误, 错误 ID：212553
        RollbackSafeTransaction("ProcessFileToDatabase_Insert")
        Return false
    }
    
    SB_SetText("已保存文件记录 - 文件: " . fileName)
    Return true
}

; ===========================================
; SavePathMerged标签（保留原有逻辑）
; ===========================================
SavePathMerged:
    Global SQLiteDB, ClipboardLock, retryCount, GlobalFileList, GlobalComment
    if (ClipboardLock) {
        if (retryCount < 3) {
            retryCount++
            SetTimer, RetrySavePathMerged, -200
            Return
        }
        retryCount := 0
        ClipboardLock := 0
        Return
    }
    ClipboardLock := 1
    retryCount := 0

    folderPath := GlobalFileList
    GlobalFileList := ""

    if (folderPath = "" || !InStr(FileExist(folderPath), "D")) {
        ClipboardLock := 0
        Return
    }

    SplitPath, folderPath, folderName
    folderState := GetFolderState(folderPath)
    totalSize := folderState.totalSize
    validFiles := folderState.fileCount
    sizeText := FormatSize(totalSize)
    Identifier := "📁"
    Format := "📁"
    textPreview := "[文件夹] 文件数量: " . validFiles . " | 大小: " . sizeText . "`n" . folderPath
    groupName := ""
    SplitPath, folderPath, , parentPath
    SplitPath, parentPath, groupName
    if (groupName = "") {
        groupName := "默认分组"
    }
    comment := GlobalComment ? GlobalComment : folderName
    HashValue := Crypt.Hash.Str(folderPath . totalSize . validFiles, "SHA256", "UTF-8")

    CheckHashSQL := "SELECT 1 FROM Database WHERE 哈希值 = ? LIMIT 1;"
    CheckHashResult := {}
    if (SQLiteDB.GetTable(CheckHashSQL, CheckHashResult, [HashValue]) && CheckHashResult.RowCount >= 1) {
        SB_SetText("文件夹内容已存在，跳过插入: " . folderName)
        ClipboardLock := 0
        Return
    }

    transactionStarted := BeginSafeTransaction("SavePathMerged_Insert")

    InsertSQL := "INSERT INTO Database (哈希值, 字节, 标签, 标识, 类型, 分组, 备注, 内容, 字符, 行数, 数量, 收藏, 禁删, 创建时间, 修改时间) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 1, 1, datetime('now', 'localtime'), datetime('now', 'localtime'));"
    InsertSuccess := false
    if (SQLiteDB.ExecuteWithParams) {
        InsertSuccess := SQLiteDB.ExecuteWithParams(InsertSQL, [HashValue, sizeText, folderName, Identifier, Format, groupName, comment, textPreview, validFiles, validFiles, validFiles])
    } else {
        EscapedHashValue := StrReplace(HashValue, "'", "''")
        EscapedSizeText := StrReplace(sizeText, "'", "''")
        EscapedFolderName := StrReplace(folderName, "'", "''")
        EscapedIdentifier := StrReplace(Identifier, "'", "''")
        EscapedFormat := StrReplace(Format, "'", "''")
        EscapedGroupName := StrReplace(groupName, "'", "''")
        EscapedComment := StrReplace(comment, "'", "''")
        EscapedTextPreview := StrReplace(textPreview, "'", "''")
        FallbackSQL := "INSERT INTO Database (哈希值, 字节, 标签, 标识, 类型, 分组, 备注, 内容, 字符, 行数, 数量, 收藏, 禁删, 创建时间, 修改时间) VALUES ('" . EscapedHashValue . "', '" . EscapedSizeText . "', '" . EscapedFolderName . "', '" . EscapedIdentifier . "', '" . EscapedFormat . "', '" . EscapedGroupName . "', '" . EscapedComment . "', '" . EscapedTextPreview . "', " . validFiles . ", " . validFiles . ", " . validFiles . ", 1, 1, datetime('now', 'localtime'), datetime('now', 'localtime'));"
        InsertSuccess := SQLiteDB.Exec(FallbackSQL)
    }

    if (!InsertSuccess) {
        ErrorMsg := SQLiteDB._ErrMsg()
        MsgBox, 16, 错误, 错误 ID：212201
        if (transactionStarted) {
            RollbackSafeTransaction("SavePathMerged_Insert")
        }
        ClipboardLock := 0
        Return
    }

    if (transactionStarted && !CommitSafeTransaction("SavePathMerged_Insert")) {
        ErrorMsg := SQLiteDB._ErrMsg()
        MsgBox, 16, 错误, 错误 ID：212209
        RollbackSafeTransaction("SavePathMerged_Insert")
        ClipboardLock := 0
        Return
    }

    SB_SetText("已保存文件夹记录 - 文件夹: " . folderName)
    ClipboardLock := 0
Return

Gosub_SavePathMerged_Success() {
    Global
    ErrorLevel := 0
    Gosub, SavePathMerged
    Return !ErrorLevel
}
